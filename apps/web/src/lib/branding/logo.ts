/** Workspace account logo — upload target dimensions and limits. */
export const ACCOUNT_LOGO_SIZE_PX = 512;
export const ACCOUNT_LOGO_MAX_BYTES = 10 * 1024 * 1024;
/** Vercel serverless request body limit — larger files use direct R2 presigned upload. */
export const ACCOUNT_LOGO_SERVER_MAX_BYTES = 4 * 1024 * 1024;
